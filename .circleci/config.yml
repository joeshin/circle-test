version: 2
jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout

      - run:
          name: Exit on postman
          command: git log --pretty="%an %s" -n1 | grep -e '^Chris Poyzer .* updated paperless$'

      - run:
          name: Greeting
          command: echo Hello, world.

      - run:
          name: Print the Current Time
          command: date
